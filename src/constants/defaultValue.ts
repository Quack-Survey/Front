const defaultFormData = {
  type: "select",
  select: [""],
  isQuater: false,
  plural: false,
  bookMark: false,
};

const defaultPluralFormData = {
  type: "select",
  select: [""],
  isQuater: false,
  plural: true,
  bookMark: false,
};

const defaultTextFormData = {
  type: "text",
  select: [""],
  isQuater: false,
  plural: false,
  bookMark: false,
};

export { defaultFormData, defaultPluralFormData, defaultTextFormData };
